{
  "name": "@bedrock/web-store-test",
  "version": "0.0.1-0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --preserve-symlinks test.js test --framework karma",
    "coverage-ci": "node --preserve-symlinks test.js test --framework karma"
  },
  "dependencies": {
    "@bedrock/core": "^6.0.1",
    "@bedrock/karma": "^6.0.0",
    "@bedrock/test": "^8.0.5",
    "@bedrock/web-store": "file:.."
  }
}
